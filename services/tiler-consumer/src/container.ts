// The container entrypoint (runs on Node inside the image, CMD ["node", "dist/container.js"]).
// Full behaviour ported in a later commit; this stub exists only so the
// package's container-side build (tsc -b tsconfig.container.json) compiles.
export {};
