{
  "$schema": "https://json.schemastore.org/tsconfig",
  "_comment": "container.ts runs on Node inside the image, not workerd, so this config extends base.json and emits dist/ for the Dockerfile's `node dist/container.js`. r2io.ts and upload-prefix.ts are included too since they're pure and shared with tests. Only `build` (tsc -b) type-checks container.ts; the package's typecheck excludes it on purpose.",
  "extends": "@internal/typescript-config/base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/container.ts", "src/r2io.ts", "src/upload-prefix.ts"],
  "exclude": ["src/**/*.test.ts"]
}
